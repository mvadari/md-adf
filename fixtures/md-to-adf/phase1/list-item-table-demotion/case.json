{
  "id": "md-to-adf.phase1.list-item-table-demotion",
  "direction": "md-to-adf",
  "description": "Converts a GFM table nested inside a list item to one plain-text paragraph per row, since ADF listItem content cannot contain table nodes.",
  "profile": "portableMarkdown",
  "options": {
    "markdownDialect": "gfm"
  },
  "features": [
    "bulletList",
    "listItem",
    "paragraph",
    "text",
    "tableDemotion"
  ],
  "expectedLoss": true,
  "skip": {},
  "xfail": {
    "python": "mistune's table plugin does not recognize GFM tables nested inside list-item content; it parses the pipe-delimited lines as a plain paragraph, so the table-demotion path this fixture targets is unreachable in Python."
  }
}
