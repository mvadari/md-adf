{
  "name": "adfmd",
  "version": "0.0.0",
  "description": "ADF-Markdown converter for JavaScript and TypeScript.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "bin": {
    "adfmd": "./dist/bin/adfmd.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist/schemas && cp ../../schemas/adf-schema.json dist/schemas/adf-schema.json",
    "format": "prettier --check \"{bin,src,test}/**/*.{ts,mjs}\" package.json README.md tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node test/conformance.test.mjs && node --test test/adf-validation.test.mjs test/options.test.mjs test/cli.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-draft-04": "^1.0.0",
    "remark-gfm": "^4.0.1",
    "remark-parse": "^11.0.0",
    "unified": "^11.0.5"
  }
}
